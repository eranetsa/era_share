# -*- coding: utf-8 -*-

from . import models

#     partner_model.some_function()
# # def forsah_pre_init_hook(cr):
#     from odoo.api import Environment

#     # Create an environment to access the Odoo models
#     env = Environment(cr, SUPERUSER_ID, {})
    
#     # Call the function from another model
#     other_model = env['crm_forsah'].search([])
#     raise UserError(other_model)
#     other_model.get_all_data()
#   env = api.Environment(cr, SUPERUSER_ID, {})
    # env['crm.forsah'].get_all_data()
#   m = env['crm.forsah']
#   raise ValidationError(m)
#   return env['crm.forsah'].get_all_data()